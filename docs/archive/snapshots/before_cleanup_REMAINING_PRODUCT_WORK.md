> Архив: исторический план или снимок. Не инструкция для текущего production и не подтверждение выполнения задач. Актуальные документы: [индекс](../../README.md).

# Оставшиеся работы по Buyerly

Снимок на 18 августа 2026 года после production commit `11cd6d6`. Источник подключения, собственные названия, заметки и последний сохранённый снимок активности кабинета закрыты в BL-027; создание копии правила из шаблона исправлено в BL-043. Эти работы в список незавершённых больше не входят.

Канонические статусы и критерии приёмки остаются в [PRODUCT_BACKLOG.md](../../PRODUCT_BACKLOG.md). Этот файл — удобная карта только незавершённых функций: что получит пользователь, из каких частей состоит работа и в каком порядке её разумно выпускать. Закрытые задачи сюда не переносятся.

## Очередь 1. Официальное подключение Meta — BL-007

В production уже выпущены кнопка `Войти через Facebook`, защищённый OAuth backend, зашифрованное подключение, поиск доступных кабинетов, группировка по BM и выборочный импорт. App Domain, exact redirect URI, Login Configuration ID, App Secret и шифрование настроены; серверная проверка Meta и диагностика OAuth пройдены. Следующий шаг — реальный вход app-role профиля и проверка найденных кабинетов. Точный чек-лист хранится в [META-AUTH-001](../../FACEBOOK_AUTHORIZATION_PLAN.md).

Оставшиеся функции:

- одноразовые invite-ссылки для открытия в нужном антидетект-профиле или отправки владельцу;
- переподключение, повторная проверка token, revoke detection и безопасное отключение;
- внесение готовых Privacy Policy, Terms of Service и Data Deletion URL в Meta Dashboard;
- реальный пилот app-role профиля, затем Advanced Access/Access Verification для внешних профилей;
- дополнительное owned/client-обогащение, если `/me/adaccounts` не покажет все ожидаемые активы.

Состав работы: [META-AUTH-001](../../FACEBOOK_AUTHORIZATION_PLAN.md).

## Очередь 2. Реальное здоровье системы — BL-050 и BL-090

Что получит пользователь и администратор:

- живые состояния API, web, bot, worker и PostgreSQL;
- начало, завершение и длительность последнего worker-цикла;
- последнюю успешную проверку каждого кабинета;
- понятное предупреждение, если worker завис, остановился или отстаёт;
- Meta latency, retry, rate-limit и ошибки синхронизации без показа секретов;
- безопасный production smoke-test, который ничего не меняет в Meta;
- регулярную проверку backup/restore и миграций.

Оставшиеся функции:

- [ ] heartbeat каждого сервиса;
- [ ] watchdog зависшего worker;
- [ ] состояние цикла по кабинетам;
- [ ] метрики внешних запросов Meta;
- [ ] production smoke без мутаций;
- [x] проверяемое восстановление резервной копии, шифрование и оффсайт-синхронизация.

## Очередь 3. Preview и безопасный запуск автоправил — BL-003, BL-040, BL-041, BL-042

Что получит пользователь:

- до сохранения правила видно, какие кампании/ad set/объявления подходят прямо сейчас;
- возле каждого условия показываются фактическое значение, оператор, порог и результат сравнения;
- режим наблюдения записывает срабатывания, но не меняет Meta;
- проверенное правило можно отдельно перевести в активное;
- minimum Spend, Impressions, Clicks и Events защищают от решений на слишком малом объёме;
- дни недели, рабочие часы и часовой пояс правила;
- обязательные floor/ceiling/cooldown для масштабирования;
- объяснение конфликта нескольких правил и запись всех подавленных правил.

Оставшиеся функции:

- [ ] preview фактических объектов и значений;
- [ ] simulation/observe-only;
- [ ] безопасная активация после наблюдения;
- [ ] minimum-data guards;
- [ ] расписание и timezone;
- [ ] защита от бюджетных циклов;
- [ ] журнал победивших и подавленных правил.

## Очередь 4. Полный каталог Meta-метрик — BL-010, BL-012, BL-020 и BL-021

Что получит пользователь:

- настраиваемый набор верхних KPI, который сохраняется за пользователем;
- сравнение KPI с предыдущим сопоставимым периодом;
- отдельный view конкретного рекламного кабинета;
- готовые виды `Закупка`, `Видео`, `Креативы`, `Автоправила`;
- расширенные события: Contacts, Subscribes, Installs, View Content, Add to Cart, Initiate Checkout и поддерживаемые custom conversions;
- стоимость каждого события;
- conversion value, purchase value и Meta ROAS;
- видео-метрики 3s, ThruPlay, 25/50/75/95/100% и стоимость просмотра;
- engagement и quality rankings, когда Meta действительно возвращает эти данные;
- точную причину, почему поле отсутствует, и контекст attribution;
- время и ошибку синхронизации отдельно по каждому кабинету.

Оставшиеся функции:

- [ ] реестр полей и action types Meta;
- [ ] ленивый запрос только видимых метрик;
- [ ] нормализация событий и стоимости;
- [ ] сохранённые KPI и account-level views;
- [ ] атрибуция и объяснение недоступных полей;
- [ ] предыдущий сопоставимый период.

## Очередь 5. Аналитическое хранилище — BL-025

Что изменится:

- сводка и таблицы читают готовые данные из PostgreSQL, а не ждут последовательных запросов в Meta при открытии;
- регулярная синхронизация работает в worker с контролем параллельности и лимитов;
- данные хранят account/entity/date/hour/source/currency/timezone/attribution;
- повторный запрос не создаёт дубли;
- временная ошибка повторяется безопасно и не стирает предыдущие данные;
- пользователь видит прогресс и частичные ошибки каждой синхронизации;
- старые почасовые факты агрегируются по согласованной политике хранения.

Оставшиеся функции:

- [ ] fact schema и миграция;
- [ ] sync jobs и checkpoints;
- [ ] идемпотентный upsert;
- [ ] freshness и retry;
- [ ] API быстрого чтения;
- [ ] retention и агрегация истории.

## Очередь 6. Иерархическая интерактивная аналитика — BL-022, BL-023 и BL-024

Что получит пользователь:

- дерево `Кабинет → Кампания → Ad set → Объявление` без перезагрузки;
- sticky header и закрепляемые первые колонки;
- массовый выбор строк;
- heatmap эффективности в текущей палитре Buyerly;
- корректная итоговая строка со взвешенными CTR/CPC/CPM;
- CSV текущего представления;
- пагинация/виртуализация для тысяч строк;
- графики Spend, CTR, CPC, CPM, CPL, CPReg и CPP;
- воронка Impressions → Link Clicks → LP Views → Leads → Registrations → Purchases;
- сравнение с предыдущим периодом;
- произвольный диапазон дат;
- breakdown по часу, географии, возрасту, полу, platform, placement и device.

Оставшиеся функции:

- [ ] иерархическая загрузка и кэширование;
- [ ] массовое выделение и итоговые расчёты;
- [ ] CSV и виртуализация;
- [ ] графики и funnel conversion;
- [ ] date range;
- [ ] совместимые Meta breakdown-комбинации.

## Очередь 7. Массовые операции с кабинетами — оставшаяся часть BL-026

Что получит пользователь:

- дополнительные фильтры по партии, байеру, статусу, валюте и активным правилам;
- назначение одного правила или группы правил сразу нескольким кабинетам;
- массовое включение/отключение назначений правил без изменения статуса рекламы;
- отдельный результат каждого кабинета при частичной ошибке;
- полный аудит инициатора и результата операции.

Оставшиеся функции:

- [ ] дополнительные фильтры по партии, байеру, статусу, валюте и активным правилам;
- [ ] bulk assignment/toggle;
- [ ] частичный результат и retry;
- [ ] AuditEvent каждой операции.

## Очередь 8. Навигация, состояния и доступность — BL-051, BL-052 и BL-090

Что получит пользователь:

- выбранная на реальных сценариях навигация, оставляющая максимум места таблицам;
- сворачиваемый sidebar или компактная верхняя панель на desktop;
- понятная mobile-навигация;
- единые loading, empty, error, partial и retry состояния;
- полноценная клавиатурная навигация и заметный focus;
- корректные диалоги с focus trap и возвратом фокуса;
- интерфейс, работающий при 200% zoom, длинных названиях и больших числах;
- browser-регрессия desktop/mobile для входа, таблиц, правил, групп и логов.

Оставшиеся функции:

- [ ] UX-проверка варианта навигации;
- [ ] полные состояния всех разделов;
- [ ] keyboard/a11y/reduced motion/contrast;
- [ ] mobile и 200% zoom;
- [ ] browser E2E основных сценариев.

## Очередь 9. Команда, роли и безопасные сессии — BL-072 и BL-073

Что получит продукт:

- законченные роли admin/buyer/viewer;
- назначение и передача кабинетов вместе с правилами и историей;
- явный выбранный владелец при административном просмотре;
- аудит просмотра и изменения чужих данных;
- ограниченные серверные web-сессии вместо бессрочного browser token;
- список устройств и завершение отдельной или всех сессий;
- rate limit входа;
- шифрование Meta token и ротация ключей;
- безопасное восстановление доступа без автоматического отзыва рабочих Meta token при обычном сбое.

Оставшиеся функции:

- [ ] viewer и матрица разрешений;
- [ ] admin context и ownership transfer;
- [ ] административный аудит;
- [ ] server sessions/devices/logout-all;
- [ ] login rate limit;
- [x] encryption at rest и key rotation.

Часть защиты Meta token выполняется раньше внутри BL-007, потому что без неё нельзя безопасно выпускать Facebook Login.

## Очередь 10. Tracker и экономика — BL-060 и BL-061

Что получит пользователь:

- первый tracker: Keitaro или Binom;
- безопасное подключение URL/API key;
- сопоставление Facebook Ad ID с сущностями tracker;
- автоматическая передача расходов и получение revenue/conversions;
- Revenue, Profit, ROI, ROAS, EPC и CR;
- отдельные Meta Purchase, First Deposit и Confirmed Deposit;
- настраиваемое главное целевое событие кабинета/оффера;
- явное разделение Meta-атрибуции и tracker-атрибуции.

Оставшиеся функции:

- [ ] выбрать Keitaro или Binom;
- [ ] connection и secret storage;
- [ ] mapping сущностей;
- [ ] cost push и stats pull;
- [ ] экономические KPI;
- [ ] проверка расхождений источников.

## Очередь 11. Креативы — BL-070

Что получит пользователь:

- галерею изображений и видео;
- объединение объявлений, использующих одинаковый креатив;
- Spend, impressions, CTR, CPC, CPL, CPReg и CPP на уровне креатива;
- раскрытие кампаний и объявлений, где он используется;
- сравнение и сортировку креативов.

Оставшиеся функции:

- [ ] получение preview/media metadata;
- [ ] безопасная дедупликация;
- [ ] агрегация performance;
- [ ] галерея и детальная панель.

## Очередь 12. Страницы и комментарии — BL-071

Что получит пользователь:

- отдельное подключение доступных Facebook Pages;
- webhook-monitoring новых комментариев;
- объединённую ленту с фильтрами;
- ответы от имени страницы;
- обратимый Hide и явно необратимый Delete;
- правила модерации и whitelist.

Оставшиеся функции:

- [ ] отдельный App Review Page permissions;
- [ ] pages selection и page token lifecycle;
- [ ] webhook verification/ingestion;
- [ ] лента, ответы, hide/unhide/delete;
- [ ] правила модерации и аудит.

Этот модуль не расширяет permissions первого релиза Facebook Login до отдельного решения.

## Очередь 13. Документация и обучение — BL-080, BL-081, BL-083 и BL-084

Что получит команда:

- полный словарь Meta action types и метрик;
- руководство сотрудника: вход, кабинеты, сводка, правила, группы, preview, логи и отмена;
- типовые безопасные рецепты правил;
- диагностику Meta/worker/database;
- onboarding-чек-лист сотрудника;
- единый поддерживаемый комплект документов без старых противоречащих инструкций;
- автоматическую проверку внутренних ссылок.

Оставшиеся функции:

- [ ] action type mapping и tracker attribution;
- [ ] employee handbook;
- [ ] operations troubleshooting;
- [ ] onboarding checklist;
- [ ] архивирование устаревших материалов;
- [ ] link checker.

## Нерешённые продуктовые вопросы

Перед соответствующими релизами владелец продукта выбирает:

1. Подключаем только собственные профили команды или также внешних владельцев через invite.
2. Автоматически выбирать все найденные кабинеты или всегда показывать подтверждение.
3. Какой tracker подключать первым: Keitaro или Binom.
4. Что означает `purchase` в рабочей воронке: покупка, первый депозит или подтверждённый депозит.
5. Нужны ли разные названия событий и воронки для разных офферов/кабинетов.
6. Срок хранения детальных почасовых данных до агрегации.
7. Attribution setting по умолчанию и возможность менять его в отчёте.
8. Должен ли admin сначала выбирать владельца или сразу видеть общую картину.
9. Когда начинать App Review разрешений для страниц и комментариев.

## Рекомендуемый ближайший порядок

1. `AUTH-00` — завтра полностью проверить Meta Dashboard.
2. `AUTH-01` — поднять и проверить Graph API.
3. `AUTH-02` — подготовить защищённую модель подключений и token.
4. `AUTH-03` — внутренний OAuth-пилот.
5. `AUTH-04` — поиск, группировка и выбор кабинетов.
6. Параллельно по продуктовой очереди: BL-050, затем BL-040—042.
7. После устойчивого подключения: BL-020/021 → BL-025 → BL-022/023/024/026.
