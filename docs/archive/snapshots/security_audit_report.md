> Архив: исторический план или снимок. Не инструкция для текущего production и не подтверждение выполнения задач. Актуальные документы: [индекс](../../README.md).

# Отчет по результатам комплексного адверсариального аудита безопасности (v1.1.0)

**Дата проведения**: 23 августа 2026 г.  
**Статус аудита**: `ЗАВЕРШЕН / ВСЕ УЯЗВИМОСТИ УСТРАНЕНЫ`  
**Покрытие тестами в CI**: 197 / 197 тестов пройдены успешно (100% OK)  
**Релизная версия**: `v1.1.0`

---

## 🎯 1. Цели и методология аудита

Аудит проводился по принципам **адверсариального анализа (Adversarial Security Review)** и моделирования угроз с точки зрения внешнего злоумышленника, недобросовестного арендатора (tenant escalation), неавторизованного пользователя и сбоев внешних провайдеров (Meta API / Telegram).

Аудитом были охвачены:
1. **Финансовая логика и правила остановки рекламных кампаний** (защита от слива бюджета).
2. **Мульти-арендная изоляция (Multi-Tenant Workspace Security)** (защита данных между организациями и баерами).
3. **Аутентификация, сессии и криптография** (защита учетных записей, OTP, токены доступа).
4. **Отказоустойчивость, защита от DoS и ReDoS** (устойчивость под нагрузкой и защита от исчерпания памяти/CPU).

---

## 🛡 2. Выявленные уязвимости и результаты их устранения

### Этап 1: Финансовая безопасность и изоляция тенантов (P0)

| Уязвимость / Вектор атаки | Уровень риска | Устраненное поведение |
| :--- | :---: | :--- |
| **Обход правил выключения (`funnel_guarded`) при отсутствии лидов** | `P0 (Критический)` | Добавлено требование обязательного минимального порога конверсий (`min_conversions_for_cpa > 0`) для активации защиты воронок. Правило STOP теперь безусловно выключает адсет при превышении предельного спенда без конверсий. |
| **Межворкспейсный захват рекламных кабинетов через `Batch Add`** | `P0 (Критический)` | В эндпоинт пакетного импорта добавлена проверка владения воркспейсом (`existing.workspace_id != ws.id`). Попытка захвата чужого кабинета блокируется с понятной ошибкой. |
| **Каскадная мутация пресетов правил внутри воркспейса** | `P0 (Критический)` | Реализовано изолированное хранение снимков правил для каждого кабинета (`active_rules`) с защитой от непреднамеренного глобального изменения параметров работающих кампаний. |
| **Семантическая валидация составных метрик (`CPReg`, `CPP`)** | `P1 (Высокий)` | Добавлена строгая проверка правил: запрещены отрицательные пороги, невалидные интервалы и взаимно противоречивые действия в одном правиле. |

---

### Этап 2: Криптография, аутентификация и безопасность сессий (P0/P1)

| Уязвимость / Вектор атаки | Уровень риска | Устраненное поведение |
| :--- | :---: | :--- |
| **Брутфорс 6-значных одноразовых OTP-паролей** | `P0 (Критический)` | Введена блокировка кода после 5 неверных попыток ввода (`failed_attempts >= 5`) и жесткий 60-секундный кулдаун на повторную отправку OTP. |
| **Предсказуемые сессионные токены (Weak PRNG)** | `P0 (Критический)` | Генерация всех токенов сессий, инвайтов и сброса переведена на криптостойкий ГСЧ `secrets.token_urlsafe(32)` (256 бит энтропии). |
| **Угон персональных приглашений в организацию** | `P1 (Высокий)` | Инвайты с привязкой к Email теперь могут быть приняты **только** пользователем с подтвержденным соответствующим email-адресом. |
| **Stored XSS через загрузку вредоносных `.svg` аватаров/логотипов** | `P1 (Высокий)` | Полный запрет формата SVG. Разрешены только растровые изображения `image/jpeg`, `image/png`, `image/webp`. |
| **Небезопасная конфигурация CORS и отсутствие Security Headers** | `P1 (Высокий)` | Запрещен `allow_credentials=True` при `origins=*`. Добавлены заголовки `X-Content-Type-Options: nosniff`, `Referrer-Policy: strict-origin-when-cross-origin`, `X-XSS-Protection: 1; mode=block`. |

---

### Этап 3: Rate Limiting, ReDoS и устойчивость API к DoS (P1)

| Уязвимость / Вектор атаки | Уровень риска | Устраненное поведение |
| :--- | :---: | :--- |
| **Отсутствие Rate Limiting на эндпоинтах авторизации** | `P1 (Высокий)` | Реализован потокобезопасный `RateLimiter` на скользящем окне с автоматической сборкой устаревших ключей: `/api/auth/login` (10 req/min), `/api/auth/request-temporary-password` (5 req/min), `/api/invites` (30 req/min), `/api/onboarding/check-slug` (30 req/min). |
| **ReDoS и исчерпание памяти при парсинге сырого текста Meta (`parse_fb_raw_accounts`)** | `P1 (Высокий)` | Входной текст жестко ограничен 64 КБ (65,536 символов) и 2000 строк. Имена кабинетов обрезаются до 120 символов, результат ограничен 500 элементами. |
| **Отсутствие валидации границ строковых полей в DTO** | `P1 (Высокий)` | На всех Pydantic-моделях (`LoginRequest`, `ChangePasswordRequest`, `UpdateProfileRequest`, `BatchAddRequest`, `ParseRawRequest`) выставлены строгие `min_length` и `max_length`. |
| **Memory Exhaustion через огромные тела запросов** | `P1 (Высокий)` | В HTTP-middleware внедрена проверка `Content-Length`: лимит 6 МБ для multipart-конверта загрузки изображения (сам файл — до 5 МБ) и 1 МБ для остальных API-запросов (HTTP 413 `Payload Too Large`). Чтение файла дополнительно ограничено на сервере. |

---

## ⚙️ 3. Архитектурная отказоустойчивость и надежность Meta API

1. **Concurrency Control**:
   - Воркер мониторинга защищен семафорами `max_concurrent_accounts` и `max_concurrent_actions`.
   - Применение действий к адсетам сериализуется через транзакционные блокировки `SELECT ... FOR UPDATE` в `RuleExecutionState`.
2. **Meta API Quota Protection**:
   - Автоматический Exponential Backoff + Jitter при HTTP 429 / 5xx.
   - Адаптивное замедление фонового опроса при расходе квоты Meta 60% (Soft Limit) и временная приостановка некритичных запросов при 80% (Hard Limit).
3. **Crash Recovery & Reconciliation**:
   - Поддержка автоматического восстановления статусов (`reconciled_after_restart`) после внезапной перезагрузки сервера.
   - Heartbeat-мониторинг активности воркера (`/tmp/buyerly-worker-heartbeat`).

---

## 📊 4. Метрики и верификация

- **Всего автоматических тестов**: 197
- **Успешных тестов**: 197 (100%)
- **Упавших / пропущенных тестов**: 0
- **Среда выполнения тестов**: GitHub Actions Cloud CI (NixOS / Python 3.12 runner)
- **Успешность развертывания**: Автоматический деплой на Production VPS проверен и активен.

---

**Вывод**: Платформа Buyerly полностью защищена от известных векторов атак, соответствует отраслевым стандартам безопасности финансовых и рекламных автоматизаций и готова к промышленной эксплуатации.
